# XPRIZE-period revenue evidence (the clean new event)

This is the **new, arms-length, in-window** revenue the competition requires: a
paid pilot/add-on of the AI Operator service, **"AI-Assisted Monthly Closing &
Report Briefing,"** contracted and paid during the XPRIZE window.

Where possible, link the invoice to the specific agent `run_id`s you delivered to
this client (see `linked_run_ids` in the manifest) so the loop is closed:
*the AI did the work* -> *the customer paid for it.*

## What goes here (redacted only)

- `pilot_agreement/` — signed pilot agreement / scope
- `invoice/` — the issued invoice for the AI Operator service
- `bank_transfer_proof/` — proof of payment (redact account numbers)
- `customer/` — redacted customer identity

Fill in `manifest.json` (copy from `manifest.example.json`). Keep it **arms-length**
(not your own entity / family). `.gitignore` blocks the real files.
